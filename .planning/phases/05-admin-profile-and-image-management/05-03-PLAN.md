---
phase: 05-admin-profile-and-image-management
plan: 03
type: execute
wave: 3
depends_on: [05-02]
files_modified:
  - scripts/minify-html.sh
  - .gitignore
  - package.json
autonomous: false
requirements: [PERF-04]

must_haves:
  truths:
    - "Running `pnpm run minify` produces apps/onlydate-dist/index.html and apps/onlydate-dist/photochoose/index.html"
    - "Minified HTML files are smaller than source files (fewer bytes)"
    - "pnpm run deploy:pages deploys from apps/onlydate-dist, not apps/onlydate"
    - "apps/onlydate-dist/ is in .gitignore and not tracked by git"
    - "Minified app loads correctly in browser: feed renders, admin panel unlocks"
  artifacts:
    - path: "scripts/minify-html.sh"
      provides: "HTML+JS+CSS minification using html-minifier-terser"
      contains: "html-minifier-terser"
    - path: ".gitignore"
      provides: "apps/onlydate-dist/ excluded from version control"
      contains: "apps/onlydate-dist/"
    - path: "package.json"
      provides: "minify script + updated deploy:pages pointing at dist"
      contains: "onlydate-dist"
  key_links:
    - from: "pnpm run minify"
      to: "apps/onlydate-dist/"
      via: "scripts/minify-html.sh called by npm script"
      pattern: "minify-html.sh"
    - from: "pnpm run deploy:pages"
      to: "apps/onlydate-dist"
      via: "wrangler pages deploy apps/onlydate-dist"
      pattern: "onlydate-dist"
---

<objective>
Set up an HTML minification pipeline using `html-minifier-terser` that shrinks the admin and public HTML files before deployment. Update the deploy script to use the minified output directory. Add a human-verify checkpoint to confirm the minified app works end-to-end before considering the phase done.

Purpose: Reduces deployed bundle size (JS + CSS + HTML whitespace elimination) without changing runtime behavior. Addresses PERF-04.
Output: `scripts/minify-html.sh`, updated `.gitignore`, updated `package.json` with `minify` and `deploy:pages` scripts, and a verified minified deployment.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-admin-profile-and-image-management/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create minification script, update .gitignore and package.json</name>
  <files>scripts/minify-html.sh, .gitignore, package.json</files>

  <read_first>
    - package.json (read current deploy:pages script before editing — the exact wrangler flags must be preserved)
    - .gitignore (read current entries before appending)
  </read_first>

  <action>
**Step 1 — Create `scripts/minify-html.sh`**

First check whether the `scripts/` directory exists at the repo root:
```bash
ls C:/CodeProjects/onlydate/scripts/ 2>/dev/null || echo "DIR MISSING"
```
If missing, it will be created by writing the file.

Create the file `scripts/minify-html.sh` with the following content exactly:

```bash
#!/bin/bash
set -e

mkdir -p apps/onlydate-dist/photochoose

echo "Minifying apps/onlydate/index.html …"
npx html-minifier-terser \
  --collapse-whitespace \
  --remove-comments \
  --minify-js true \
  --minify-css true \
  apps/onlydate/index.html \
  -o apps/onlydate-dist/index.html

echo "Minifying apps/onlydate/photochoose/index.html …"
npx html-minifier-terser \
  --collapse-whitespace \
  --remove-comments \
  --minify-js true \
  --minify-css true \
  apps/onlydate/photochoose/index.html \
  -o apps/onlydate-dist/photochoose/index.html

echo "Minification complete."
echo "Source index.html:          $(wc -c < apps/onlydate/index.html) bytes"
echo "Minified index.html:        $(wc -c < apps/onlydate-dist/index.html) bytes"
echo "Source photochoose:         $(wc -c < apps/onlydate/photochoose/index.html) bytes"
echo "Minified photochoose:       $(wc -c < apps/onlydate-dist/photochoose/index.html) bytes"
```

Important flags to NOT include:
- Do NOT add `--process-scripts` — can corrupt SortableJS CDN tag
- Do NOT add `--remove-optional-tags` — may break browsers
- Do NOT add `--collapse-boolean-attributes` — breaks `<input multiple>`

Make the script executable:
```bash
chmod +x C:/CodeProjects/onlydate/scripts/minify-html.sh
```

**Step 2 — Update `.gitignore`**

Read the current `.gitignore`. The existing content is:
```
node_modules/
.wrangler/
dist/
.env
.env.cloudflare
*.local
```

Append `apps/onlydate-dist/` as a new line at the end of the file.

**Step 3 — Update `package.json`**

Read the current `package.json`. The current scripts are:
```json
{
  "deploy:worker": "export CLOUDFLARE_API_TOKEN=$(grep '^CLOUDFLARE_API_TOKEN=' .env.cloudflare | cut -d= -f2-) && export CLOUDFLARE_ACCOUNT_ID=$(grep '^CLOUDFLARE_ACCOUNT_ID=' .env.cloudflare | cut -d= -f2-) && pnpm --filter onlydate-worker run deploy",
  "deploy:pages": "export CLOUDFLARE_API_TOKEN=$(grep '^CLOUDFLARE_API_TOKEN=' .env.cloudflare | cut -d= -f2-) && export CLOUDFLARE_ACCOUNT_ID=$(grep '^CLOUDFLARE_ACCOUNT_ID=' .env.cloudflare | cut -d= -f2-) && npx wrangler pages deploy apps/onlydate --project-name onlydate --branch main --commit-dirty=true",
  "deploy": "pnpm run deploy:worker && pnpm run deploy:pages",
  "typecheck": "pnpm --filter onlydate-worker run typecheck"
}
```

Make two changes:
1. Add a `"minify"` script that runs the shell script
2. Change `deploy:pages` to minify first, then deploy from `apps/onlydate-dist`

Updated scripts block:
```json
{
  "deploy:worker": "export CLOUDFLARE_API_TOKEN=$(grep '^CLOUDFLARE_API_TOKEN=' .env.cloudflare | cut -d= -f2-) && export CLOUDFLARE_ACCOUNT_ID=$(grep '^CLOUDFLARE_ACCOUNT_ID=' .env.cloudflare | cut -d= -f2-) && pnpm --filter onlydate-worker run deploy",
  "minify": "bash scripts/minify-html.sh",
  "deploy:pages": "export CLOUDFLARE_API_TOKEN=$(grep '^CLOUDFLARE_API_TOKEN=' .env.cloudflare | cut -d= -f2-) && export CLOUDFLARE_ACCOUNT_ID=$(grep '^CLOUDFLARE_ACCOUNT_ID=' .env.cloudflare | cut -d= -f2-) && pnpm run minify && npx wrangler pages deploy apps/onlydate-dist --project-name onlydate --branch main --commit-dirty=true",
  "deploy": "pnpm run deploy:worker && pnpm run deploy:pages",
  "typecheck": "pnpm --filter onlydate-worker run typecheck"
}
```

Note: `pnpm run minify &&` is prepended to the wrangler command so dist is always fresh before deploy. The wrangler deploy path changed from `apps/onlydate` to `apps/onlydate-dist`.

**Step 4 — Run the minifier to validate**

```bash
cd C:/CodeProjects/onlydate && pnpm run minify
```

This should print byte counts for both files. Confirm:
- `apps/onlydate-dist/index.html` exists and is smaller than `apps/onlydate/index.html`
- `apps/onlydate-dist/photochoose/index.html` exists and is smaller than `apps/onlydate/photochoose/index.html`

If `html-minifier-terser` is not installed globally, `npx` will download it automatically. On first run this may take ~10 seconds.
  </action>

  <verify>
    <automated>test -f C:/CodeProjects/onlydate/apps/onlydate-dist/index.html && echo "EXISTS" && grep -c "onlydate-dist" C:/CodeProjects/onlydate/package.json && grep -c "apps/onlydate-dist/" C:/CodeProjects/onlydate/.gitignore</automated>
  </verify>

  <acceptance_criteria>
    - `test -f scripts/minify-html.sh` → true
    - `test -f apps/onlydate-dist/index.html` → true (minifier ran successfully)
    - `test -f apps/onlydate-dist/photochoose/index.html` → true
    - `grep -c "onlydate-dist" package.json` → ≥ 2 (minify script + deploy:pages)
    - `grep -c "apps/onlydate-dist/" .gitignore` → 1
    - Minified index.html byte count is LESS THAN source index.html byte count (confirmed by script output)
  </acceptance_criteria>

  <done>Minification script exists, runs without error, produces smaller output files, deploy:pages uses dist directory, and apps/onlydate-dist/ is gitignored.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    HTML minification pipeline (scripts/minify-html.sh) that produces apps/onlydate-dist/ from the source HTML files. The deploy:pages script now minifies first and deploys from the dist directory.

    Files produced:
    - apps/onlydate-dist/index.html (public mini app)
    - apps/onlydate-dist/photochoose/index.html (admin panel)

    All Phase 5 backend and frontend features are also complete:
    - POST /feed-entry/update endpoint (edit display_name, handle, cover_url)
    - POST /feed-entry/photo/toggle-hidden endpoint
    - R2 error logging in delete handlers
    - Atomic D1 batch delete for feed-entry/delete
    - Feed_entry photos served in public profile (filtered by is_hidden=0)
    - Edit modal in admin UI
    - Hide/unhide toggle on feed_entry gallery photos
    - Client-side canvas WebP resize before upload
  </what-built>

  <how-to-verify>
    **Step 1 — Verify minified file sizes**
    ```bash
    wc -c apps/onlydate/index.html apps/onlydate-dist/index.html
    wc -c apps/onlydate/photochoose/index.html apps/onlydate-dist/photochoose/index.html
    ```
    Expected: dist files are smaller (typically 20-40% reduction).

    **Step 2 — Open minified public app in browser**
    Open `apps/onlydate-dist/index.html` in a browser (or deploy to staging). Verify:
    - The feed grid loads (or shows the app shell without errors)
    - Browser console shows no JavaScript errors
    - SortableJS CDN script tag is not broken (no 404 in Network tab)

    **Step 3 — Open minified admin panel**
    Open `apps/onlydate-dist/photochoose/index.html` in a browser. Verify:
    - Lock screen appears with password input
    - After entering password, persona list loads
    - Edit button (✏) appears on feed_entry rows
    - No console errors

    **Step 4 — Test Edit modal end-to-end (against live API)**
    Using the deployed worker (or `wrangler dev`):
    - Click Edit on any feed_entry row
    - Modify display_name, click Save
    - Confirm the row updates immediately without page refresh
    - Confirm the change appears on the public feed

    **Step 5 — Test feed_entry photo hide**
    - Open a feed_entry's photo grid
    - Click the eye icon on a gallery photo to hide it
    - Confirm the photo card gets the "hidden" overlay
    - Visit the public profile URL — hidden photo should NOT appear

    **Step 6 — Test canvas resize (optional, requires image upload)**
    - In the admin panel, upload a large image (>800px) to a feed_entry gallery
    - In browser DevTools Network tab, check the upload request
    - The uploaded file should be image/webp (or image/png if Safari fallback triggered)
    - The dimensions in R2 should be ≤800px on the longest side
  </how-to-verify>

  <resume-signal>
    Type "approved" when all steps pass, or describe any issues found.
    If minified app has JS errors, check if html-minifier-terser mangled the SortableJS event handler (the fix is to add --no-collapse-inline-tag-whitespace to the script).
  </resume-signal>
</task>

</tasks>

<verification>
After Task 1 automated checks:

1. `test -f scripts/minify-html.sh` → true
2. `test -f apps/onlydate-dist/index.html` → true
3. `test -f apps/onlydate-dist/photochoose/index.html` → true
4. `grep -c "onlydate-dist" package.json` → ≥ 2
5. `grep -c "apps/onlydate-dist/" .gitignore` → 1
6. `grep "catch(() => {})" apps/onlydate-worker/src/routes/admin.ts` → no output (from Plan 01)
7. `grep -c "resizeToWebP" apps/onlydate/photochoose/index.html` → ≥ 3 (from Plan 02)

Phase 5 complete after human checkpoint passes.
</verification>

<success_criteria>
- scripts/minify-html.sh exists and produces apps/onlydate-dist/ output
- Minified files are smaller than source (byte count comparison)
- package.json has "minify" script and updated deploy:pages pointing at apps/onlydate-dist
- apps/onlydate-dist/ is gitignored
- Human verifier confirms minified admin panel and public app work correctly end-to-end
</success_criteria>

<output>
After completion, create `.planning/phases/05-admin-profile-and-image-management/05-03-SUMMARY.md`
</output>
